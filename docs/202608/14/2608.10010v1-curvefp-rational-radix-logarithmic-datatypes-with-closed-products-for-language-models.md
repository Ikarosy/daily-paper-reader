---
title: "CurveFP: Rational-Radix Logarithmic Datatypes with Closed Products for Language Models"
title_zh: CurveFP：用于语言模型的具有封闭乘积的有理基数对数数据类型
authors: Ye Qiao
date: 2026-08-08
pdf: "https://arxiv.org/pdf/2608.10010v1"
tags: ["query:wbv"]
score: 9.0
evidence: 面向低比特语言模型的闭包乘积码本量化
tldr: 低精度数据类型用于降低语言模型成本，但多数格式只优化标量保真度，乘积运算未加改变。CurveFP提出封闭乘积码本族，将量化幅度分布到交错对数曲线上，采用有理基数调节动态范围与局部分辨率，使乘积只需符号异或和整数索引更新。七位CurveFP在四个7B-9B模型上超过FP8困惑度且少一位，八位在全部36个GEMM中降低NMSE并完成3B-token预训练。该工作实现了FP8级数值行为、七位推理和更简单的乘积路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.10010v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1635, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10010v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1576, \"height\": 743, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10010v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1651, \"height\": 730, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.10010v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1620, \"height\": 699, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.10010v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1139, \"height\": 239, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10010v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 797, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10010v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 903, \"height\": 191, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10010v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 959, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.10010v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1401, \"height\": 202, \"label\": \"Table\"}]"
motivation: 现有低精度格式未设计乘积的闭包性，导致乘积运算复杂且精度损失。
method: 通过有理基数和交错对数曲线构造封闭乘积码本，使乘积变为符号异或与整数索引更新。
result: 七位在4个模型上优于FP8困惑度，八位在36个GEMM中降低NMSE且预训练性能更好。
conclusion: 证明算术协同设计可兼顾精度与效率，实现FP8级数值行为、七位推理和更简乘积路径。
---

## 摘要
低精度数据类型能降低语言模型成本，但大多数格式在优化标量保真度的同时，并未改变其乘积诱导的算术运算。我们引入了 CurveFP，一个封闭乘积码本族，它在紧凑的块尺度下将量化幅度分配到交错的对数曲线上。有理基数根据局部分辨率调整动态范围，而均匀的曲线索引使每个非零乘积在代数上闭合。乘积的形成变为精确的符号异或（XOR）和整数索引更新，并且推导出的有限相位计数决定了累加调度。我们将该代数实例化为用于训练的 CurveFP 8（E4C3/E5C2）和用于紧凑部署的 CurveFP 7（E3C3）。评估中，CurveFP 7 在四个 7B–9B 模型上使用少一个元素比特，在困惑度上优于按张量量化的 FP8，并保持在原生质量的 1.32% 以内。CurveFP 8 在所有 36 组成对的前向和反向 GEMM 比较中均降低了操作数的 NMSE。在三个匹配的 128.3M 参数三元组中，每种模式在每个种子下完成 3B 令牌的预训练；CurveFP 8 的平均 BF16 推理困惑度达到 22.5366，而 FP8 为 22.5407，并且在所有三个种子中都产生了更低的格式引起的惩罚。一个 36 格的下游评估矩阵发现，在所有 12 个种子-格式比较中，CurveFP 8 训练的检查点在 WikiText-103 困惑度上更低，而 PG-19 和任务差异则好坏参半。总之，这些结果确立了 CurveFP 作为一种算术协同设计，它结合了 FP8 级别的数值行为、七位推理和显著更简单的乘积路径。

## Abstract
Low-precision datatypes reduce language-model cost, but most formats optimize scalar fidelity while leaving the arithmetic induced by their products unchanged. We introduce CurveFP, a closed-product codebook family that distributes quantized magnitudes across interleaved logarithmic curves under compact block scales. A rational radix tunes dynamic range against local resolution, while uniform curve indices make every nonzero product algebraically closed. Product formation becomes an exact sign XOR and integer-index update, and a derived finite phase count determines the accumulation schedule. We instantiate this algebra as CurveFP eight E4C3/E5C2 for training and CurveFP seven E3C3 for compact deployment. In evaluation, CurveFP seven beats tensor-wise FP8 perplexity on four 7B--9B models with one fewer element bit and stays within 1.32\% of native quality. CurveFP eight lowers operand NMSE in all 36 paired forward and backward GEMM comparisons. Across three matched 128.3M-parameter triplets, every mode completes 3B-token pretraining per seed; CurveFP eight reaches mean BF16-inference perplexity 22.5366 versus 22.5407 for FP8 and incurs a lower format-induced penalty in all three seeds. A 36-cell downstream matrix finds lower WikiText-103 perplexity for the CurveFP eight-trained checkpoints in all 12 seed-format comparisons, with mixed PG-19 and task deltas. Together, these results establish CurveFP as an arithmetic co-design that combines FP8-class numerical behavior, seven-bit inference, and a substantially simpler product path.

---

## 论文详细总结（自动生成）

# CurveFP：用于语言模型的具有封闭乘积的有理基数对数数据类型

## 1. 核心问题与研究动机

论文针对的核心问题是：**低精度数据类型在优化标量（单个数值）表示精度的同时，忽略了乘积运算的算术结构**。具体而言：

- **背景**：低精度算术已成为扩展语言模型训练和推理规模的主要杠杆。FP8 证明了角色特定的指数/尾数布局可以匹配 16 位训练质量；微缩放（microscaling）格式通过逐块尺度分摊范围；后训练量化方法（如 SmoothQuant、QuaRot）从数据侧缓解离群值问题。
- **问题**：尽管上述方法使量化更精确，但其元素乘积仍需要传统的整数或浮点乘法器。**乘积结构是低精度设计中未被充分利用的效率来源**。
- **现有方案的缺陷**：
  - 均匀整数简化了累加，但对激活范围扩展敏感，依赖细粒度尺度。
  - 对数表示使乘法变为加法，但粗粒度的 2 的幂次牺牲了局部分辨率。
  - 加性 2 的幂次码通过多个移位-加项恢复更多电平，但增加了复杂度。
- **核心诉求**：需要一个比单纯标量误差更强的目标——**联合设计电平几何与乘积代数**，使更低精度同时改善表示密度和算术规则性。

## 2. 方法论

### 2.1 核心思想

CurveFP 是一个**块缩放的封闭乘积码本族**。它将量化幅度分布到 K 条交错对数曲线上，每个非零元素存储符号、二进制补码指数和曲线索引，共享一个 2 的幂次块尺度。**均匀曲线间距使乘法在码域上精确闭合**：符号异或、曲线索引加法、带进位的指数加法。这样乘积形成无需通用乘法器、查找表或投影操作。

### 2.2 关键技术细节

**（1）有理基数数据类型族**

非零量化值表示为：

$$b_x = (-1)^s 2^{a_b} r^{e + k/K}$$

其中：s 为符号，$a_b$ 为共享尺度指数，$r = 2^{p/q}$ 为有理基数，e 为指数（E 位），k 为曲线索引（C 位），K = 2^C。元素占用 1 + E + C 位，保留零作为哨兵值。

**（2）封闭乘积与相位结构化累加**

两个非零操作数的乘积通过以下规则形成：
- 符号：$s_p = s_x \oplus s_w$（异或）
- 曲线索引：$k_p = (k_x + k_w) \bmod K$
- 进位：$c = \lfloor (k_x + k_w)/K \rfloor$
- 指数：$e_p = e_x + e_w + c$
- 尺度：$a_p = a_x + a_w$

**（3）相位计数定律**

由 $r^{e+k/K} = 2^{p(eK+k)/(qK)}$，令 N = qK，整数 n = eK + k 推进时，分数指数为 pn mod N 除以 N。p 在 Z_N 中生成的加法子群的阶为：

$$H = \frac{qK}{\gcd(p, qK)}$$

H 即相位类数量，决定了点积所需的累加调度复杂度。点积可写为：

$$b_x^T b_w = \sum_{h=0}^{H-1} 2^{h/H} \sum_t n_{h,t} 2^t$$

其中 $n_{h,t}$ 为有符号整数计数。每个乘积精确路由到按二进制移位索引的整数计数，归约后仅剩 H 个固定相位权重。

**（4）具体格式**

| 格式 | 位数 | E | C | 正电平数 | 相位数 H | 作用 |
|------|------|---|---|----------|----------|------|
| CurveFP7 E3C3 | 7 | 3 | 3 | 63 | 8 | 紧凑推理 |
| CurveFP8 E4C3 | 8 | 4 | 3 | 127 | 8 | 前向训练 |
| CurveFP8 E5C2 | 8 | 5 | 2 | 127 | 4 | 反向梯度 |

前向用 E4C3（精度优先），梯度用 E5C2（范围优先），镜像 FP8 E4M3/E5M2 的分配。块缩放以紧凑二进制尺度指数保持移位-计数累加结构。

## 3. 实验设计

### 3.1 数据集与基准

- **后训练量化推理**：WikiText-2 测试集（2,048 token 滚动对数似然协议），覆盖四个模型架构：Llama-3-8B、Qwen3-8B、Falcon-H1-7B、Qwen3.5-9B。
- **训练 GEMM 诊断**：Pythia-410M 和 Llama-3.2-3B 的真实前向/反向矩阵乘积窗口。
- **从零预训练**：128.3M 参数 Llama 风格解码器，在 FineWeb-Edu 的 2,999,943,168 个 token 上训练。
- **OOD 评估**：WikiText-103、PG-19；零样本/五样本任务：HellaSwag、PIQA、ARC-Easy/Challenge、WinoGrande、LAMBADA、BLiMP。

### 3.2 对比方法

- BF16/FP16（全精度基准）
- FP8 E4M3/E5M2（标准八位浮点基线）
- 微缩放格式（microscaling）
- 带实值尺度的有符号整数（INT8）
- CurveFP7 / CurveFP8（本文方法）

所有格式共享相同的 GEMM 内核和评估代码，隔离数据类型和优化效应。

## 4. 资源与算力

论文**未明确报告** GPU 型号、数量、训练时长等具体算力信息。文中仅说明：
- 预训练实验为 9 条训练通道（3 种模式 × 3 个种子），每条通道 30,517 步更新，序列长度 1024，全局批量大小 96，共约 30 亿 token。
- 模型规模为 128.3M 参数，表明实验在相对较小的规模下进行，但具体硬件资源配置未披露。

## 5. 实验数量与充分性

### 实验数量概览

1. **推理质量实验**：4 个模型 × 多种量化配置（tensorwise、rowwise、G64/G512 等多种粒度），含完整的 Falcon-H1 粒度扫描。
2. **有理基数消融**：3 种基数（r=2、r=2^(8/9)、r=2^(3/4)）× 4 个模型。
3. **训练 GEMM 诊断**：2 个模型 × 3 种 GEMM 类型（前向、激活梯度、权重梯度）× 多层采样 = 36 组成对比较。
4. **从零预训练**：3 种模式（BF16、FP8、CurveFP8）× 3 个种子 = 9 条完整 3B-token 训练通道。
5. **OOD/下游评估矩阵**：2 个 OOD 语料 + 多个任务 × 3 种训练模式 × 3 个种子，共 36 格比较。

### 充分性与公平性评估

- **优点**：多种子重复、匹配初始化与数据顺序、共享评估代码、训练/推理算术分离（BF16 推理评估）等设计提升了实验的客观性。
- **不足**：预训练仅在 128.3M 参数规模上进行，未验证更大模型（如 1B+）上的训练行为；OOD 任务结果中 PG-19 和下游任务的种子方差较大，部分结论仅支持"迁移平价"而非"普遍优越"。

## 6. 主要结论与发现

1. **CurveFP7 实现七位推理**：在 G64 下，CurveFP7 在全部 4 个模型上优于 tensorwise FP8，困惑度保持在原生质量的 1.32% 以内；在 Falcon-H1 上，从 G512 粒度开始超越 INT8，且随尺度共享扩大优势扩大（tensorwise 下 6.71 vs 10.10 PPL）。
2. **有理基数验证了质量-相位控制律**：更密基数（r=2^(3/4)）在所有模型上取得最佳困惑度，但需要 32 个相位类；二进制基数（8 相位）是主要操作点，相位-质量权衡符合公式 H = qK/gcd(p, qK)。
3. **CurveFP8 在全部 36 个训练 GEMM 比较中降低 NMSE**：聚合 NMSE 比 FP8 低 8.2–11.1%，且优势不局限于前向推理。
4. **完整 3B-token 预训练无发散**：CurveFP8 的训练格式 PPL 为 22.6054（FP8 为 22.6164），BF16 推理 PPL 为 22.5366（FP8 为 22.5407）；在所有 3 个种子中，CurveFP8 的格式诱导惩罚更低（0.0688 vs 0.0758 PPL）。
5. **迁移与格式转移**：CurveFP8 训练的检查点在 WikiText-103 的所有 12 个种子-格式比较中优于 FP8 训练；匹配训练与推理算术可将格式惩罚降低约 43–50%。

## 7. 优点与亮点

### 方法层面
- **乘积代数闭合性**：首次将乘积结构与表示设计联合优化，乘积形成仅需 XOR 和整数加法，无需通用乘法器。
- **显式相位-复杂度契约**：公式 H = qK/gcd(p, qK) 将基数选择与累加成本直接关联，为架构设计提供明确的权衡依据。
- **统一代数族**：同一封闭乘法规则同时服务训练（E4C3/E5C2）和推理（E3C3），无需额外投影或查找。
- **架构无关的算术规范**：算法层面完整定义操作、状态边界和输出语义，不依赖特定加速器。

### 实验层面
- 多模型、多粒度、多种子、多任务的全面评估矩阵。
- 区分"训练时算术"和"推理时算术"，单独隔离训练质量与推理量化损失。
- 配对比较（同种子、同数据、同初始化）确保了统计效力。

## 8. 不足与局限

1. **规模局限**：预训练实验仅覆盖 128.3M 参数模型，未在大规模（≥1B）模型上验证训练可行性；推理评估虽覆盖 7B–9B 模型，但仅限于后训练量化。
2. **统计不确定性**：PG-19 和下游任务上种子方差大于平均增益，作者仅主张"迁移平价"而非"普遍优越"；最大单种子综合误差分数为 |z| = 1.43，显著性有限。
3. **硬件指标缺失**：论文未报告实际加速比、面积、延迟或能耗指标，仅提供算法层面的复杂度说明；"乘法器消除"的实际收益缺乏端到端硬件验证。
4. **精度-相位权衡的边际收益递减**：从 8 相位增加到 32 相位仅带来 0.0055–0.0259 的 PPL 改善，却增加 4 倍相位类，实际部署中性价比存疑。
5. **应用限制**：静态操作数使用重建 MSE 搜索尺度，动态操作数使用 ceil-absmax 缩放，这些启发式是否在更宽泛的分布（如非 Transformer 架构或极端动态范围）下保持鲁棒性未充分探讨。
6. **未披露算力成本**：缺乏训练基础设施信息，影响可复现性和成本评估。

## 9. 总结

CurveFP 通过将表示设计与乘积代数联合优化，展示了低精度算术协同设计的可行路径。它在保持 FP8 级数值行为的同时，实现了七位推理和显著简化的乘积路径。尽管存在规模验证不足和统计显著性有限的局限，其封闭乘积代数和显式相位契约的设计理念为低精度 LLM 训练/推理提供了有吸引力的新方向。

（完）
