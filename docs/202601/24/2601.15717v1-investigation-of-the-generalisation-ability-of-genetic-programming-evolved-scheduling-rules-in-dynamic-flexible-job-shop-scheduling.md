---
title: Investigation of the Generalisation Ability of Genetic Programming-evolved Scheduling Rules in Dynamic Flexible Job Shop Scheduling
title_zh: 动态柔性作业车间调度中遗传规划演化调度规则的泛化能力研究
authors: "Luyao Zhu, Fangfang Zhang, Yi Mei, Mengjie Zhang"
date: 2026-01-22
pdf: "https://arxiv.org/pdf/2601.15717v1"
tags: ["keyword:EOH", "keyword:EAA"]
score: 9.0
evidence: 遗传编程用于自动演化调度启发式算法
tldr: 本研究系统探讨了遗传编程（GP）演化的调度规则在动态柔性作业车间调度（DFJSS）中的泛化能力。针对现有研究多局限于同类实例测试的现状，本文从问题规模、车间参数及数据分布等维度分析了GP规则在未见实例上的表现。研究发现，决策点分布的相似性是决定泛化性能的关键，为开发更具鲁棒性的自动化调度算法提供了理论依据。
motivation: 现有研究缺乏对GP演化调度规则在不同结构特征的DFJSS实例间泛化能力的深入分析。
method: 通过在不同机器数量、作业规模、利用率水平和数据分布的多维度实验，系统评估GP规则的跨类型泛化表现。
result: 实验证明，训练与测试实例在规模或参数上的相似性，以及决策点分布的一致性，能显著提升规则的泛化效果。
conclusion: 本研究揭示了影响GP规则泛化性的核心因素，强调了在演化过程中考虑异构实例以提升调度规则通用性的必要性。
---

## 摘要
动态柔性作业车间调度（DFJSS）是一个复杂的组合优化问题，需要在动态生产环境中同时进行机器分配和工序排序决策。遗传规划（GP）已被广泛应用于自动演化 DFJSS 的调度规则。然而，现有研究通常在相同类型的 DFJSS 实例上训练和测试 GP 演化的规则，这些实例仅在随机种子而非结构特征上有所不同，导致其跨类型泛化能力在很大程度上尚未得到探索。为填补这一空白，本文系统地研究了 GP 演化调度规则在多种 DFJSS 条件下的泛化能力。本文在多个维度上进行了一系列实验，包括问题规模（即机器和工件的数量）、关键车间参数（如利用率水平）以及数据分布，以分析这些因素如何影响 GP 在未见实例类型上的性能。结果表明，当训练实例包含比测试实例更多的工件（同时保持机器数量固定），以及当训练和测试实例具有相似的规模或车间参数时，会出现良好的泛化效果。进一步分析表明，DFJSS 实例中决策点的数量和分布在解释这些性能差异方面起着至关重要的作用。相似的决策点分布会带来更好的泛化能力，而显著的差异则会导致性能明显下降。总之，本研究为 DFJSS 中 GP 的泛化能力提供了新的见解，并强调了演化出能够有效处理异构 DFJSS 实例的更具泛化性的 GP 规则的必要性。

## Abstract
Dynamic Flexible Job Shop Scheduling (DFJSS) is a complex combinatorial optimisation problem that requires simultaneous machine assignment and operation sequencing decisions in dynamic production environments. Genetic Programming (GP) has been widely applied to automatically evolve scheduling rules for DFJSS. However, existing studies typically train and test GP-evolved rules on DFJSS instances of the same type, which differ only by random seeds rather than by structural characteristics, leaving their cross-type generalisation ability largely unexplored. To address this gap, this paper systematically investigates the generalisation ability of GP-evolved scheduling rules under diverse DFJSS conditions. A series of experiments are conducted across multiple dimensions, including problem scale (i.e., the number of machines and jobs), key job shop parameters (e.g., utilisation level), and data distributions, to analyse how these factors influence GP performance on unseen instance types. The results show that good generalisation occurs when the training instances contain more jobs than the test instances while keeping the number of machines fixed, and when both training and test instances have similar scales or job shop parameters. Further analysis reveals that the number and distribution of decision points in DFJSS instances play a crucial role in explaining these performance differences. Similar decision point distributions lead to better generalisation, whereas significant discrepancies result in a marked degradation of performance. Overall, this study provides new insights into the generalisation ability of GP in DFJSS and highlights the necessity of evolving more generalisable GP rules capable of handling heterogeneous DFJSS instances effectively.

---

## 论文详细总结（自动生成）

### 动态柔性作业车间调度中遗传规划演化调度规则的泛化能力研究总结

#### 1. 核心问题与研究背景
*   **核心问题**：研究通过遗传规划（Genetic Programming, GP）自动演化的调度规则在动态柔性作业车间调度（DFJSS）中的**跨类型泛化能力**。
*   **研究背景**：DFJSS 要求同时解决机器分配和工序排序。虽然 GP 已被证明能有效演化调度规则，但现有研究大多在相同特征（仅随机种子不同）的实例上进行训练和测试。本论文旨在探索当测试环境的规模、参数或数据分布发生变化时，GP 演化的规则是否依然有效。

#### 2. 方法论
*   **核心思想**：利用 GP 作为超启发式（Hyper-heuristic）方法，自动演化出一对调度规则：**路由规则（Routing Rule）**用于机器分配，**排序规则（Sequencing Rule）**用于工序选择。
*   **关键技术细节**：
    *   **多树表示法**：每个 GP 个体包含两棵树，分别代表上述两种规则。
    *   **目标函数**：最小化平均加权逾期时间（Mean Weighted Tardiness, $WT_{mean}$），公式为：
        $WT_{mean} = \frac{1}{|J|} \sum_{J_{ij} \in J} w_{ij} \times \max\{0, C_{ij} - d_{ij}\}$
        其中 $w_{ij}$ 是权重，$C_{ij}$ 是完成时间，$d_{ij}$ 是交货期。
    *   **终端集与函数集**：终端集包含机器负载、工序处理时间、作业权重等特征；函数集包括 $\{+, -, *, /, \max, \min\}$。
*   **决策点分析**：引入“决策点分布”概念，通过计算不同实例间机器负载分布的**重叠率（Overlap Ratio）**来量化环境相似度。

#### 3. 实验设计
*   **实验场景**：
    1.  **问题规模**：改变工件数量（100-10000）和机器数量（2-50）。
    2.  **车间参数**：改变系统利用率（0.50-0.99）、交货期因子（1.0-4.0）和批次大小（单件到巨大批次）。
    3.  **参数分布**：使用指数分布、伽马分布、对数正态分布、正态分布和均匀分布生成数据。
*   **Benchmark（基准）**：以“同类型训练”为基准（即训练和测试使用相同配置的实例），对比跨配置测试的表现。
*   **对比方法**：主要对比不同训练配置下的 GP 模型在异构测试集上的性能差异。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件型号（如 GPU/CPU）或训练时长。
*   **常规配置**：根据 GP 实验的惯例，通常在 CPU 集群上运行。实验设置种群大小为 500，演化 100 代，每组实验独立运行 30 次以确保统计显著性。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大量组合实验，涵盖了规模、参数、分布三个维度的交叉测试。
*   **充分性与客观性**：
    *   每组配置均进行了 **30 次独立重复实验**。
    *   使用 **Wilcoxon 秩和检验**（显著性水平 0.05）进行统计分析，区分性能的“显著更好”、“显著更差”或“无显著差异”。
    *   实验设计系统且全面，通过热力图和重叠率矩阵深入分析了性能退化的根本原因，具有较高的客观性。

#### 6. 论文的主要结论与发现
*   **规模效应**：在机器数量固定时，在**大规模（工件多）**实例上训练的规则能很好地泛化到小规模实例，因为大规模实例提供了更丰富的决策场景；但改变机器数量会显著降低泛化性。
*   **参数敏感性**：利用率、交货期和批次大小的改变都会影响性能。规则在相似参数环境下表现较好，在极端差异环境下性能大幅下降。
*   **分布决定论**：数据分布的改变对泛化能力打击最大。GP 规则高度依赖于训练时的特定概率分布。
*   **核心发现**：泛化能力的强弱取决于**决策点分布的相似性**。决策点分布重叠率越高，泛化性能越强。

#### 7. 优点
*   **系统性强**：首次对 DFJSS 中 GP 规则的泛化能力进行了多维度的系统性评估。
*   **理论深度**：不仅观察到性能变化，还通过“决策点分布重叠率”这一指标从本质上解释了泛化失效的原因。
*   **实用价值**：提出了“分布相似性可作为泛化预测器”的观点，为未来无需仿真即可评估规则适用性提供了可能。

#### 8. 不足与局限
*   **目标单一**：实验仅针对最小化平均加权逾期时间（$WT_{mean}$），未探讨多目标调度下的泛化情况。
*   **缺乏新算法**：论文侧重于“调查与分析”，并未提出一种能够显著提升泛化能力的新型 GP 变体算法。
*   **静态分布假设**：实验中的分布虽然多样，但在单一训练过程中分布是静态的，未考虑生产过程中分布发生漂移的极端动态情况。

（完）
