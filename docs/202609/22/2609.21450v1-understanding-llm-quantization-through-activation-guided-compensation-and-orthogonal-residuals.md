---
title: Understanding LLM Quantization through Activation-Guided Compensation and Orthogonal Residuals
title_zh: 从激活引导补偿与正交残差理解LLM量化
authors: "Yamato Narita, Issei Sato"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21450v1"
tags: ["query:wbv"]
score: 8.0
evidence: 面向LLM低比特量化的W4A4权重激活误差分解
tldr: 针对LLM后训练W4A4量化中激活离群值降低有效量化分辨率的问题，本文把局部权重-激活量化误差精确分解为激活引导的权重补偿项与正交残差，并用持续通道离群量和常规激活量给出残差上界。基于该上界，作者推导随机Hadamard旋转、符号选择与通道缩放的实用准则，解释随机符号抑制离群通道相长干涉、多样本符号提升变换选择，以及二阶矩平衡导出L2缩放并松弛得到SmoothQuant式L∞缩放。在八个Llama和Mistral模型上以无反向传播配置评估，性能与梯度训练的SpinQuant相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.21450v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 654, \"height\": 342, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.21450v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 521, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21450v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 740, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21450v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1447, \"height\": 1394, \"label\": \"Table\"}]"
motivation: W4A4后训练量化受激活离群值制约，权重优化、通道缩放与正交旋转虽有效，但各自处理的误差成分及其关系尚不清晰。
method: 将局部权重-激活量化误差精确分解为激活引导权重补偿与正交残差，用持续离群和常规激活量界定残差，并据此推导Hadamard旋转、符号选择与通道缩放准则。
result: 在八个Llama与Mistral模型上以无反向传播配置评估，性能与梯度训练的SpinQuant相当。
conclusion: 该分解统一解释了权重补偿与变换设计的作用边界，为免训练W4A4量化提供可操作准则。
---

## 摘要
训练后权重-激活量化可降低大语言模型的显存和推理成本，但激进的W4A4量化仍然困难，因为激活离群值会降低有效量化分辨率。尽管权重优化、逐通道缩放和正交旋转可以缓解这一问题，但它们所处理的误差分量及其关系仍不清晰。通过将局部权重-激活量化误差精确分解为激活引导的权重补偿项和正交残差，我们利用持续存在的逐通道离群量与常规激活量对残差进行界定。该分解阐明了哪些误差分量可通过权重补偿处理，哪些需要变换设计。随后，我们利用残差界推导出应用随机Hadamard旋转、符号选择和通道缩放的实用准则。特别地，该分析解释了随机符号如何抑制持续离群通道之间的相长干涉，采样多个符号模式如何改进变换选择，以及二阶矩平衡如何导出 $L_2$ 缩放规则，而进一步松弛则恢复 SmoothQuant 风格的 $L_\infty$ 缩放。我们在八个Llama和Mistral模型上通过无需反向传播的配置评估这些准则，获得了与经梯度训练的SpinQuant相当的性能。

## Abstract
Post-training weight-activation quantization reduces the memory and inference costs of large language models, but aggressive W4A4 quantization remains difficult because activation outliers degrade effective quantization resolution. Although weight optimization, channel-wise scaling, and orthogonal rotation mitigate this problem, the error components they address and their relationship remain unclear. Using an exact decomposition of local weight-activation quantization error into an activation-guided weight compensation term and an orthogonal residual, we bound the residual using persistent channel-wise outlier and regular activation quantities. This decomposition clarifies which error components can be addressed by weight compensation and which require transformation design. We then use the residual bounds to derive practical guidelines for applying randomized Hadamard rotation, sign selection, and channel scaling. In particular, the analysis explains how random signs suppress constructive interference among persistent outlier channels, how sampling multiple sign patterns can improve transformation selection, and how second-moment balancing leads to an $L_2$ scaling rule while a further relaxation recovers SmoothQuant-style $L_\infty$ scaling. We evaluate these guidelines through backpropagation-free configurations across eight Llama and Mistral models, obtaining performance competitive with gradient-trained SpinQuant.

---

## 论文详细总结（自动生成）

# 论文总结：从激活引导补偿与正交残差理解 LLM 量化

## 1. 核心问题与整体含义
- **背景**：大语言模型（LLM）部署成本高，训练后量化（PTQ）可用少量校准数据降低显存与推理成本。
- **核心难题**：激进的 **W4A4**（4-bit 权重 + 4-bit 激活）仍困难，因为激活离群值会扩大量化范围、降低普通值的有效分辨率；其中“持续逐通道离群值”（persistent channel-wise outliers）尤其会反复主导逐 token 激活量化尺度。
- **现有方法分散**：GPTQ 类做权重重建优化，SmoothQuant 类做通道缩放，QuaRot/SpinQuant 类做正交旋转。但它们分别处理哪一类量化误差、彼此如何互补，缺乏统一解释。
- **论文目标**：通过局部权重-激活量化误差的精确分解，区分“可由权重补偿处理的误差”和“必须由变换设计处理的误差”，并据此推导旋转、
